bazel_dep(name = "gazelle", version = "0.47.0")
bazel_dep(name = "rules_go", version = "0.60.0")

go_sdk = use_extension("@rules_go//go:extensions.bzl", "go_sdk")
go_sdk.download(
    name = "go_sdk",
    version = "1.26.4",
)
use_repo(go_sdk, "go_sdk")

register_toolchains("@go_sdk//:all")

go_deps = use_extension("@gazelle//:extensions.bzl", "go_deps")
go_deps.from_file(go_mod = "//:go.mod")

# discord-mcp is itself a Bazel workspace whose hand-written BUILD files load rules
# this module does not carry; regenerate them from scratch inside the go_deps repo.
go_deps.gazelle_override(
    build_file_generation = "clean",
    path = "github.com/justinswe/discord-mcp",
)
use_repo(
    go_deps,
    "com_github_bwmarrin_discordgo",
    "com_github_jackc_pgx_v5",
    "com_github_justinswe_discord_mcp",
    "com_github_justinswe_std",
    "com_github_modelcontextprotocol_go_sdk",
    "com_github_nats_io_nats_go",
    "com_github_spf13_cobra",
    "com_github_stretchr_testify",
    "com_github_valkey_io_valkey_go",
    "com_google_cloud_go_pubsub_v2",
    "org_golang_google_genai",
    "org_golang_google_protobuf",
    "org_golang_x_net",
    "org_modernc_sqlite",
    "org_uber_go_zap",
)
